{
  "$schema": "https://example.com/agent-ready-config-schema.json",
  "defaults": {
    "output": "human",
    "minScore": 0,
    "failOnStatuses": ["fail", "error"]
  },
  "scan": {
    "output": "human",
    "minScore": 50,
    "failOnStatuses": ["fail", "error"]
  },
  "doctor": {
    "output": "human",
    "minScore": 50,
    "failOnStatuses": ["fail", "error"]
  },
  "init": {
    "framework": "hono",
    "preset": "application",
    "features": ["api-catalog", "robots", "sitemap", "llms", "markdown", "mcp", "agent-card", "oauth-discovery", "oauth-protected-resource"],
    "dryRun": false,
    "output": "human"
  }
}
